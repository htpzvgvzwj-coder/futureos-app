ANTHROPIC_API_KEY=
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Optional fallback providers for Future Mirror's debate turn - tried in
# order (anthropic -> groq -> gemini) only when the provider ahead of them
# fails to respond (network/rate-limit/credit exhaustion). Leave unset to
# keep the current anthropic-only behavior.
GROQ_API_KEY=
GROQ_MODEL=
GEMINI_API_KEY=
GEMINI_MODEL=
